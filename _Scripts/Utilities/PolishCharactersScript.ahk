#SingleInstance
#NoTrayIcon
~a & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ą"
    else
        Send "{BackSpace}ą" 
}  
~c` & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ć"
    else
        Send "{BackSpace}ć"
}  
~e & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ę"
    else
        Send "{BackSpace}ę" 
}  
~l & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ł"
    else
        Send "{BackSpace}ł" 
}  
~n & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ń"
    else
        Send "{BackSpace}ń" 
}  
~o & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ó"
    else
        Send "{BackSpace}ó" 
}  
~s & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ś"
    else
        Send "{BackSpace}ś" 
}  
~x & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ź"
    else
        Send "{BackSpace}ź" 
}  
~z & Down::{
    if GetKeyState("Shift")
        Send "{BackSpace}Ż"
    else
        Send "{BackSpace}ż" 
}  

; a, ą, b, c, ć, d, e, ę, f, g, h, i, j, k, l, ł, m, n, ń, o, ó, p, (q), r, s, ś, t, u,
; (v), w, (x), y, z, ź, ż.